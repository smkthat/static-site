export default function greet(name) {
  /**
   * A function that format greeting message.
   *
   * @param {string} name - The name to greet.
   * @return {string} Formatted string with name.
   */

  return `Hello, ${name}!`;
}
